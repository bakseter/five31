apiVersion: v1
kind: Service
metadata:
  name: five31-backend
  namespace: five31
  labels:
    app.kubernetes.io/name: five31-backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: five31-backend
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8080
